model: qwen2.5:14b-instruct
model_fast: qwen2.5:14b-instruct
model_big: qwen2.5:14b-instruct
prefer_fast: true
big_triggers:
  - deep
  - long
  - essay
  - synthesize
  - thorough
  - in depth
  - detailed
  - analysis
# Effective precedence: LOCAL_AGENT_OLLAMA_BASE_URL / OLLAMA_BASE_URL env,
# then this config value, then the built-in default below.
ollama_base_url: http://127.0.0.1:11434
max_tokens: 800
max_tokens_big_second: 4500
temperature: 0.2
timeout_s: 300
timeout_s_big_second: 600
max_chars_full_read: 200000
security:
  allowed_roots:
    - "runs/"
    - "allowed/"
  allowed_exts:
    - ".md"
    - ".txt"
    - ".json"
  deny_absolute_paths: true
  deny_hidden_paths: true
  allow_any_path: false
  auto_create_allowed_roots: true
  roots_must_be_within_security_root: true
workroot: "../local-agent-workroot/"
corpus:
  db_path: "index/index.sqlite"
  sources:
    - name: corpus
      root: "allowed/corpus/"
      kind: corpus
    - name: scratch
      root: "allowed/scratch/"
      kind: scratch
  max_chars: 1200
  overlap: 120
embeddings:
  db_path: "embeddings/db/embeddings.sqlite"
  provider: "torch"
  model_id: "sentence-transformers/all-MiniLM-L6-v2"
  preprocess: "obsidian_v1"
  chunk_preprocess_sig: ""
  query_preprocess_sig: ""
  batch_size: 64
  torch:
    local_model_path: ""
    cache_dir: ""
    device: "auto"
    dtype: "float16"
    batch_size: 64
    max_length: 512
    pooling: "mean"
    normalize: true
    trust_remote_code: false
    offline_only: true
retrieval:
  lexical_k: 40
  vector_k: 40
  vector_fetch_k: 60
  rel_path_prefix: ""
  fusion: "simple_union"
grounding:
  evidence_top_n: 15
  citation_validation:
    enabled: true
    strict: false
    require_in_snapshot: true
    heading_match: "prefix"
    normalize_heading: true
runs:
  log_evidence_excerpts: true
  max_total_evidence_chars: 200000
  max_excerpt_chars: 1200
memory:
  db_path: "memory/durable.sqlite"
  enabled: true
